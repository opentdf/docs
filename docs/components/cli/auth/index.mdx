---
title: otdfctl auth
---

import React from 'react';
import CommandLineDocs from '@site/src/components/CommandLineDocs';

<CommandLineDocs {...JSON.parse("{\"title\":\"Manage local authentication session\",\"command\":{\"name\":\"auth\"},\"fullCommand\":\"otdfctl auth\"}")}>

> [!NOTE]
> Requires experimental profiles feature. (Linux not yet supported. Windows is brittle.)

The auth commands facilitate the process of authenticating the user with the system using profiles to store the
credentials.

</CommandLineDocs>
          